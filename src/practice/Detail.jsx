export default function Signup() {

    return (
        <form onSubmit={
            () => 
                alert("Submitting......")}>
                    < input />
                    <button>send</button> 
        </form>
    );
}